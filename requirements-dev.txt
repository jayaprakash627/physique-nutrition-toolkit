# Test-only dependencies. The app itself needs nothing beyond requirements.txt.
-r requirements.txt

pytest          # the test runner
httpx           # required by fastapi.testclient for the API tests

# Browser tests (tests/e2e/). Opt-in — `pytest` alone never needs these, which
# keeps a fresh clone one command away from a green suite. Run them with
# `pytest -m e2e`, after a one-off `playwright install chromium`.
playwright      # drives a real Chromium
pytest-playwright   # the page/browser fixtures
